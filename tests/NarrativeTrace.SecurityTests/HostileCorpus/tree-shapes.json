{
  "$comment": "TraceNode call-tree shapes: depth and cycles in the tree structure itself (TraceNode.Children), as opposed to graphs.json's object-graph shapes fed to the value renderer. A builder in each port turns a case into a live TraceTree. See README.md.",
  "cases": [
    { "id": "chain-200", "description": "a two-hundred-node call chain - ordinary, well within TreeWalk.MaxDepth", "kind": "chain", "n": 200 },
    { "id": "self-cycle", "description": "a node whose own children list holds itself", "kind": "cycle", "n": 1 },
    { "id": "cycle-two-nodes", "description": "a calls b, b calls a", "kind": "cycle", "n": 2 },
    { "id": "cycle-hundred-nodes", "description": "a hundred-node ring", "kind": "cycle", "n": 100 },
    { "id": "chain-beyond-max-depth", "description": "deeper than TreeWalk.MaxDepth - the depth-limit case", "kind": "chain", "n": 2100 }
  ]
}
