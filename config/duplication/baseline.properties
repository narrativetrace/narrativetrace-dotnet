# Duplication baseline (jscpd 5.2.0, 60-token floor, identifiers/literals ignored) — main sources
# only; the test tree is reported by `DuplicationReport` but never gates (see
# documentation/duplication.md). `DuplicationCheck` fails when the measured main percentage rises
# more than 0.3 points above `main.percent`, or when a non-exempt cluster is larger than
# `main.largestCluster` — see config/duplication/exemptions.txt for what is exempt and why.
#
# Recorded from the first scan, floored to measured (the same idiom this build already uses for
# coverage and mutation-score floors): lower this with the commit that removes the duplication,
# never raise it without a reasoned exemption line instead.
# main.percent still counts exempt data-table rows (jscpd measures raw duplication regardless of the
# ratchet's exemptions; unchanged by the 2026-09-12 exemption wave since the same 137 clusters are
# still present, just reclassified between exempt and non-exempt).
# main.largestCluster is the largest NON-exempt cluster: after the 2026-09-12 ruling exempted the
# TraceNamer/GenericTokenDetector/RedactionPolicy word tables and the CanonicalEntry/SpanContext wide
# records (config/duplication/exemptions.txt), the largest surviving finding is
# MermaidSequenceRenderer.cs ↔ PlantUmlSequenceRenderer.cs (242 tokens) — genuine parallel-renderer
# logic, left as a finding rather than exempted since this wave's ruling did not cover it (unchanged
# by the 2026-09-13 SequenceDiagramRenderers dispatch below — that commit removed a different,
# below-floor construction cluster, not this one).
#
# 2026-09-13: NarrativeTestBase/NarrativeFixture/TraceOutputWriter's independent
# "new MermaidSequenceRenderer.Render, new PlantUmlSequenceRenderer.Render" construction (mirroring
# the Java runtime's SequenceDiagramRenderers ruling) was replaced with one shared
# SequenceDiagramRenderers.Mermaid/PlantUml dispatch; main measured 12.7% (floored from 12.8%).
#
# 2026-09-13 (later the same day): the MermaidSequenceRenderer.cs ↔ PlantUmlSequenceRenderer.cs
# cluster this file used to name as the largest non-exempt finding is gone — mirroring the Java
# runtime's SequenceWalk/SequenceGrammar/DiagramLabel refactor, the shared traversal now lives in
# SequenceWalk.cs, the per-format literals in {Mermaid,PlantUml}SequenceGrammar.cs, and the
# sanitized-text micro-type in DiagramLabel.cs; both renderers' Render methods collapsed to a
# one-line call into SequenceWalk.Render. Nothing in src/NarrativeTrace.Diagrams clusters above the
# floor against another Diagrams file any more. main measured 11.1% (floored from 12.7%); the new
# largest non-exempt cluster is unrelated pre-existing duplication —
# src/NarrativeTrace.Core/JsonEscape.cs ↔ src/NarrativeTrace.Glossary/JsonEscape.cs (238 tokens),
# which simply ranked second while the renderer-pair cluster stood above it.
#
# 2026-09-13 (a third pass the same day): the JsonEscape.cs ↔ JsonEscape.cs cluster this file named
# above is gone. Glossary already carries a ProjectReference on Core (for CanonicalEntryMapper via
# Runtime), so this was a genuine same-direction duplicate (both sides escape-on-write, byte-identical
# behavior) sitting in a project graph where the fold is free — not an inverse escape/parse pair and
# not a project barred from depending on Core, so no exemption applies. Folded into Core's internal
# JsonEscape (already the escaper Core's own ScenarioManifest used), reached from Glossary via a new
# `InternalsVisibleTo(NarrativeTrace.Glossary)` on Core — the smallest-surface option, since the type
# was already internal on both sides and neither caller needed it public. GlossaryJsonWriter and
# GlossaryUsageReport (Glossary's two callers) now call Core's JsonEscape.Escape and their existing
# tests are unchanged. This mirrors the Java runtime's own instance of this same finding, which was
# ruled a zero-dependency-CLI exemption instead (a project there is barred from depending on the
# module that would host the shared copy) — and it is the same shape the Python port's glossary
# already used (glossary delegates to the shared escaper rather than keeping its own copy). main
# measured 10.8% (floored from 11.1%); the new largest non-exempt cluster is unrelated pre-existing
# duplication — src/NarrativeTrace.Testing.NUnit/NarrativeSuiteReport.cs ↔
# src/NarrativeTrace.Testing.Xunit/NarrativeSuiteFixture.cs (177 tokens), which simply ranked next
# while the JsonEscape pair stood above it.
recorded=2026-09-13
commit=2026-09-13-json-escape-core-glossary-fold
main.percent=10.8
main.largestCluster=177
