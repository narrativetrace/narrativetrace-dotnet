<!-- source: README.md blob 70ce4bfcb35c | translated: 2026-09-07 | reviewed: - -->
# NarrativeTrace .NET

[English](README.md) | [Español](LEAME.md) | [Português](LEIAME.md) | **简体中文**

**把你的代码*做过什么*变成一个可以阅读的故事。** NarrativeTrace 把方法执行
记录成一条叙事追踪 — 一份嵌套的、人类可读的记述，涵盖一个工作单元背后的
调用、参数、结果与耗时 — 并为你的命名*清晰度*评分，让不可读的代码在上线前
就被标记出来。

它采用迁移优先的架构：同一套库可运行在现代 .NET、`netstandard2.0` 以及遗留的
`net48` 上。

## 问题

这个方法有一半是日志噪音：

```csharp
public OrderResult PlaceOrder(string customerId, string productId, int quantity)
{
    _logger.LogInformation("Placing order for {Customer} product {Product} qty {Qty}",
        customerId, productId, quantity);

    var customer = _customers.FindCustomer(customerId);
    var unitPrice = _catalog.LookupPrice(productId);
    _logger.LogDebug("Priced {Product} at {Price}", productId, unitPrice);

    _inventory.Reserve(productId, quantity);
    var payment = _payments.Charge(customerId, unitPrice * quantity, $"tok_{customer.Id}");
    _logger.LogInformation("Payment processed: {Txn}", payment.TransactionId);
    return new OrderResult(NextOrderId(), payment.TransactionId, unitPrice * quantity, quantity);
}
```

业务逻辑只有寥寥几行；日志又是另外几行。每个开发者写日志的方式都不一样 —
消息、级别、包含的值各不相同。结果就是不一致、冗长，而且与它所描述的代码
纠缠在一起。

NarrativeTrace 把这些都去掉。只写纯粹的业务逻辑：

```csharp
public OrderResult PlaceOrder(string customerId, string productId, int quantity)
{
    var customer = _customers.FindCustomer(customerId);
    var unitPrice = _catalog.LookupPrice(productId);
    _inventory.Reserve(productId, quantity);
    var payment = _payments.Charge(customerId, unitPrice * quantity, $"tok_{customer.Id}");
    return new OrderResult(NextOrderId(), payment.TransactionId, unitPrice * quantity, quantity);
}
```

追踪会自动从方法名、参数名和返回值生成 — 也就是本来就存在的信息：

```
OrderService.PlaceOrder(customerId: "cust-1", productId: "book-123", quantity: 2)
  CustomerService.FindCustomer(customerId: "cust-1") → Customer { Id = cust-1, Name = Ada Lovelace, Tier = Premium }
  ProductCatalogService.LookupPrice(productId: "book-123") → 9.99
  InventoryService.Reserve(productId: "book-123", quantity: 2)
  PaymentService.Charge(customerId: "cust-1", amount: 19.98) → PaymentConfirmation { TransactionId = txn-00001, Amount = 19.98 }
→ OrderResult { OrderId = ORD-00001, TransactionId = txn-00001, Total = 19.98, Quantity = 2 }
```

这个流程就是真实的
[`NarrativeTrace.Examples.ECommerce`](examples/NarrativeTrace.Examples.ECommerce)
示例，由其特征测试守护，防止回归。

## 当出错的时候

追踪让 bug 无所遁形。给一位客户扣款超过其额度，故事就会正好停在出问题的
地方：

```
OrderService.PlaceOrder(customerId: "cust-1", productId: "book-123", quantity: 2)
  CustomerService.FindCustomer(customerId: "cust-1") → Customer { Id = cust-1, … }
  ProductCatalogService.LookupPrice(productId: "book-123") → 9.99
  InventoryService.Reserve(productId: "book-123", quantity: 2)
  PaymentService.Charge(customerId: "cust-1", amount: 19.98) !! PaymentDeclinedException: Amount 19.98 exceeds approval limit 10
!! PaymentDeclinedException: Amount 19.98 exceeds approval limit 10
```

`Reserve` 被调用了，却没有出现与之对应的 `Release` — 泄漏的库存预留在追踪
里一目了然，而不是埋在某个日志文件中。

## 追踪的质量取决于你的命名

同一个"玩家加入世界"的流程，被追踪了两次 — 一次用领域命名，一次用泛化命名
([Minecraft 命名演示](examples/NarrativeTrace.Examples.Minecraft))：

**重构后(干净的命名)：**
```
WorldServer.PlayerJoined(playerName: "Steve")
  WorldGenerator.GenerateChunk(x: 0, z: 0) → Chunk { X = 0, Z = 0, Biome = plains }
  PlayerInventory.AddItem(item: "wooden_pickaxe", count: 1) → true
  CraftingTable.Craft(recipe: "wooden_pickaxe") → "wooden_pickaxe"
  CreatureSpawner.SpawnHostile(creatureType: "zombie", x: 10, y: 64, z: 20) → "zombie"
```

**未重构(泛化的命名)：**
```
GameManager.Handle(input: "Steve")
  DataProcessor.Process(a: 0, b: 0) → "0,0"
  StateManager.Update(key: "wooden_pickaxe", value: 1) → true
  ThingFactory.Create(spec: "wooden_pickaxe") → "wooden_pickaxe"
  EntityHandler.Execute(type: "zombie", a: 10, b: 64, c: 20) → "zombie"
```

调用图完全相同，返回值完全相同 — 只有命名不同。清晰度分析器给干净版本的
评分严格更高(一个特征测试同时锁定了结构的一致性与评分差距)。如果你的代码
讲不出自己的故事，它就需要重构。

## 这对 AI 辅助开发为何重要

每一行 `_logger.LogInformation(...)` 都是 AI 编程工具 — Claude Code、
Copilot、Cursor — 必须解析、消耗 token 并围绕其推理的一行。在典型的服务类
中，日志占据 30–50% 的代码行。去掉它们，你会得到：

- **每个上下文窗口装下更多业务逻辑** — 同样的 token 预算覆盖更多真正的
  代码。
- **更清晰的推理** — 模型看到的是代码*做了什么*，而不是它如何记录自己做了
  什么。
- **只含信号的 diff** — pull request 展示的是逻辑变更，而不是逻辑与日志
  混在一起的变更。

## 与其他方案的对比

- **结构化日志**(`Microsoft.Extensions.Logging`)要求你亲手*写*日志语句。
  NarrativeTrace 从代码结构生成叙事 — 没有 `LogInformation` 调用，没有消息
  模板，也不用接线作用域。当请求追踪处于活动状态时，它还会填充 `traceId`
  和一个确定性的、人类可读的追踪名称。
- **分布式链路追踪**(OpenTelemetry、Jaeger)用 Span 跟踪请求在服务之间的
  流转。NarrativeTrace 捕获的是*方法级*调用树，带完整的参数值与返回值 —
  这是基于 Span 的追踪器不会记录的细节。`NarrativeTrace.Observability` 包
  连接两者：把树导出为 `Activity` Span(批量)，或通过
  `OtelTraceEventListener` 实时导出。
- **拦截器/AOP 日志**(`DispatchProxy`、Castle)会自动记录进入/退出，但产出
  的是扁平、机械的输出。NarrativeTrace 产出嵌套的调用树，并在此之上为你的
  命名质量评分。

它不替代生产告警或服务拓扑图；它提供的是两者都给不了的东西 — 一份可读的
执行叙事，同时充当代码质量诊断。

## 它不会取代你的日志框架

NarrativeTrace 没有自己的 sink、provider 或投递管道。你的
`Microsoft.Extensions.Logging` 配置(Serilog、NLog、Application
Insights，以及你已经在用的 sink 和 enricher)都会照常运行，不受影响。

它取代的是手写的叙事*语句* — 比如
`_logger.LogInformation("Placing order for {Customer}...",
customerId)` 这样的代码行。被追踪的方法会自动产出那份叙事，并通过这些
代码本该使用的同一个 `ILogger` 抽象发出：接入 `NarrativeTrace.Logging`
的桥接(`AddNarrativeLogging()`，或 `LoggingNarrativeContext` 装饰
器)，每一次进入/退出都会变成一条普通的结构化日志记录 — 同样的
`LoggerMessage` 机制，同样的 `BeginScope`。

手动日志和 NarrativeTrace 可以自由混用 — 把两者指向同一个 `ILogger`，
它们就会共享宿主已经配置好的每一个 sink、filter 和 enricher。哪里还想
要一条 `_logger.LogWarning(...)`，照写就是。

## 你能得到什么

- **执行叙事** — 把服务包进追踪 proxy，每次调用都会被捕获成一棵
  `进入 → 结果` 事件树，带参数、返回值、异常与耗时。
- **多种渲染** — 同一份追踪可渲染为缩进文本、Markdown、散文、Mermaid /
  PlantUML 时序图、规范 JSON 或 OpenTelemetry Span。
- **清晰度评分** — 由 NLP 驱动的分析器为类/方法/参数命名打分(动词、缩写、
  内聚性、泛化 token)，并把问题呈现出来。
- **框架集成** — ASP.NET Core 请求中间件、DI 自动包装、`ILogger` 作用域、
  OpenTelemetry(批量 + 实时)，以及在测试失败时打印叙事的 xUnit / NUnit
  测试助手。
- **工具链** — `dotnet-narrativetrace` CLI(扫描程序集、按清晰度门禁)以及把
  它接入你构建流程的 `NarrativeTrace.MSBuild` 包。

## 本地试用

无需项目，无需接线 — 运行仓库自带的示例，现场观看叙事的发生：

```bash
./demo.sh --example ecommerce --no-pause    # 旗舰级的服务调用图
./demo.sh --example ecommerce --classic     # 同一次运行，以带时间戳的日志行呈现
./demo.sh --list                            # 本仓库自带的每一个示例
```

完整的选择器见下方的[演示](#演示)，
[最初 10 分钟](documentation/first-10-minutes.md)(仅英文)则一步步演示如何
把追踪加到你自己的一个服务上，并在每一步给出真实输出。

## 把它加进一个测试

从“一个有趣的库”到“我看到了自己代码的一份追踪”之间，最省事的路径就是：
把测试主体包进一个 fixture，在测试失败时打印出这个故事。

**xUnit** — 包裹测试主体(xUnit 不会把测试结果交给 fixture)：

```csharp
public class OrderTests : IClassFixture<NarrativeFixture>
{
    private readonly NarrativeFixture _fixture;
    public OrderTests(NarrativeFixture fixture) => _fixture = fixture;

    [Fact]
    public void Places_an_order() => _fixture.Run(nameof(Places_an_order), ctx =>
    {
        var orders = NarrativeTraceProxy.Create<IOrderService>(new OrderService(), ctx);
        Assert.Equal("confirmed", orders.PlaceOrder("book-123", 2));
    });
}
```

**NUnit** — 从 `NarrativeTestBase` 派生；失败会在 teardown 中通过
`TestContext` 自动检测并叙述。

在 `dotnet test` 之前设置 `NARRATIVETRACE_OUTPUT=true`，两者都会把真实文件
写到磁盘上(`traces/<Class>/<slug>.md`、一个同名的 `.json`、一张 `.mmd`
图，以及一个不含值的 `.nt`) — [最初 10 分钟](documentation/first-10-minutes.md)
(仅英文)完整走一遍这套流程，包括重命名一个方法、然后看着清晰度评分下降。

## 选择你的集成方式

| 你想要 | 从哪里开始 |
|---|---|
| 在普通 .NET 中对包装对象拥有显式控制权 | 下方的 `NarrativeTraceProxy.Create<T>` |
| 每一个按命名空间匹配到的接口服务都被自动追踪 | 下方的依赖注入 |
| ASP.NET Core 中生产环境的 HTTP 请求生命周期 | 下方的 ASP.NET Core 中间件 |
| 在测试中获得追踪，接线最少 | 上方的“把它加进一个测试” |
| 一个 CI 命名质量门禁，无需运行测试 | [CLI](#cli-dotnet-narrativetrace) / MSBuild |

完整的对照表和决策图见
[选择集成方式](documentation/choosing-an-integration.md)(仅英文)。

**Proxy — 显式控制，适用于任何 .NET 应用：**

```csharp
using NarrativeTrace.Core;
using NarrativeTrace.Proxy;
using NarrativeTrace.Runtime;

var context = new SyncNarrativeContext(new NarrativeTraceConfig(TracingLevel.Detail));
IOrderService orders = NarrativeTraceProxy.Create<IOrderService>(new OrderService(), context);
orders.PlaceOrder("book-123", quantity: 2);

Console.WriteLine(IndentedTextRenderer.Render(context.CaptureTrace()));
```

```
OrderService.PlaceOrder(sku: "book-123", quantity: 2) → "confirmed"
  PaymentService.Charge(amount: 19.98) → "approved"
  InventoryService.Reserve(sku: "book-123", quantity: 2) → true
```

**依赖注入 — 自动包装实现类位于某个命名空间前缀之下的所有接口**，是
Spring/Micronaut Bean 追踪在 .NET 中的等价物(请把它放在最后调用，等它应该
看到的每一个服务都已经注册完毕之后)：

```csharp
services.AddNarrativeTracing(o =>
{
    o.Level = TracingLevel.Detail;
    o.Namespaces("MyApp.Services", "MyApp.Domain");
});
```

**ASP.NET Core — 按请求生成追踪，在请求结束时导出：**

```csharp
builder.Services.AddNarrativeTrace(builder.Configuration);

var app = builder.Build();
app.UseMiddleware<NarrativeTraceMiddleware>(); // 靠近管道的最外层

app.MapGet("/orders/{id}", (string id, HttpContext http) =>
    NarrativeTraceProxy.Create<IOrderService>(new OrderService(), http.GetNarrativeContext())
        .FindOrder(id));
```

导出器与用户上下文见
[ASP.NET Core 集成指南](documentation/guides/zh-CN/ASP.NET-Core集成指南.md)，
CLI、MSBuild 以及日志/OpenTelemetry 桥接见
[安装指南](documentation/guides/zh-CN/安装指南.md)。

**渲染格式** — 与你如何捕获追踪无关，可以独立选择：

```csharp
MarkdownRenderer.Render(trace);          // Markdown 报告
IndentedTextRenderer.Render(trace);      // 适合控制台 / 日志
ProseRenderer.Render(trace);             // 叙事散文
MermaidSequenceRenderer.Render(trace);   // Mermaid 时序图
PlantUmlSequenceRenderer.Render(trace);  // PlantUML 时序图
JsonExporter.Export(trace, metadata);    // 规范 JSON(按 schema 校验)
```

## 隐私与安全，一屏看懂

这个库运行在你的进程内部，并会写出团队会互相分享的文件。以下内容都是对照
代码验证过的，不是假设：

| 保证 | 如何做到 |
|---|---|
| **脱敏在每一种已发布的集成中都是无条件的** | `[NotTraced]` 与一份 26 项的名称拒绝列表(外加与字段名无关的 JWT / 支付卡 / `Set-Cookie` 取值形态检测)同样适用于 proxy 捕获、DI 自动包装、ASP.NET Core 中间件、测试输出以及模板占位符。没有任何开关能把它们关掉。 |
| **`[NotTraced]` 始终优先** | 用在属性或字段上时，它的 getter 根本不会被调用；用在参数上时，该值永远不会被渲染。它优先于定制的 `ToString()`，也胜过确实存在、但没有任何已发布集成会启用的 `RedactionPolicy.Disabled` 后备开关(escape hatch)。 |
| **AI 安全的结构化产物不含任何值** | `.nt` 文件(以及可选开启的 `.structural.json`)只包含名称、层级结构与结果种类 — 一个属性测试会向每一个值字段注入恶意内容，只要有任何一点泄漏出来就会失败。 |
| **追踪失败不会导致你的应用失败** | 捕获与渲染在每一个风险点上都做了异常隔离 — 抛异常的 `ToString()`、`[NarrativeSummary]` 成员或模板路径 getter 都会退化为占位符，而不会影响你业务调用的结果。 |
| **资源使用是有界的** | 字符串长度、集合大小、对象宽度与嵌套深度都设了上限，并且循环检测与深度无关。 |

两个诚实的局限：检测是基于名称/形态的，不是统计式的(没有“看起来像随机值”
这类启发式判断 — 藏在无关痛痒字段名下的密钥不会被抓到)，而且模板占位符
解析(`[Narrated]`/`[OnError]`)始终使用默认的拒绝列表，即使你已经在别处给
`ValueRenderer` 接入了自定义的 `RedactionPolicy`。

→ [隐私与脱敏](documentation/privacy-and-redaction.md)(仅英文)获取完整
的、逐行的契约。

## 性能

追踪确实要做事，也确实有成本 — 这里不做“零开销”的宣称。回归门禁
(BenchmarkDotNet)会在相对于已提交基线**变慢超过 15%**或**任何**分配量
增加时让构建失败。以下是记录下来的基线数字，`Detail` 级别(默认级别)，
取自
[`benchmarks/benchmark-baseline.json`](benchmarks/benchmark-baseline.json)：

| 基准 | 平均耗时 | 已分配 |
|---|---|---|
| `CoreBenchmarks.EnterExitCycle` | ~2.5 µs | 2368 B |
| `CoreBenchmarks.RenderObject` | ~819 ns | 1611 B |
| `RendererBenchmarks.MarkdownSmall` | ~595 ns | 2704 B |
| `ConcurrencyBenchmarks.ForkJoin_TwoTasks` | ~3.9 µs | 5602 B |

在 `TracingLevel.Off` 下，上下文会短路并且什么也不捕获 — 一个特征测试锁定
了 off 级别的上下文会产出空追踪这一事实 — 但这条路径目前还没有被单独做
基准测试，所以“Off 在架构上是零捕获”这一说法目前只是“经测试验证”，而不是
一个测得的 ns/op 数字。

## 哪些免费，哪些是 Pro 版

**免费版**是本仓库中的一切 — 在 BSL 1.1 下源码可得，可免费用于生产环境：
整条捕获管道、每一种渲染器与导出格式、清晰度评分、以上的每一种集成方式，
以及不含值的结构化产物。目前被 Pro 版把关的功能(不在本仓库中发布，已迁移
到 `narrative-trace-dotnet-enterprise`)：跨运行的事件流聚合
(`EventAggregator`)；以及已规划、但尚未构建的流程摘要、迁移差异、运行时
依赖图，以及面向 AI agent 的 MCP 工具处理器。
[功能指南](documentation/feature-guide.md)(仅英文)是权威的状态表：每个
功能都标注为 免费、Pro、开发中 或 已规划，并附有每个已发布行背后的代码。

## 包

| 包 | 提供什么 |
|---|---|
| `NarrativeTrace.Core` | 追踪模型、上下文、渲染器、清晰度类型、配置，以及 `NarrativeTrace.Core.Annotation` 中的特性(Attribute) |
| `NarrativeTrace.Runtime` | 上下文实现、事件管道、JSON/章节导出器 |
| `NarrativeTrace.Proxy` | 基于 `DispatchProxy` 的追踪 proxy，外加 `[Traced]` |
| `NarrativeTrace.DependencyInjection` | `AddNarrativeTracing` 按命名空间自动包装 |
| `NarrativeTrace.AspNetCore` | 请求生命周期中间件 + `ITraceExporter` SPI |
| `NarrativeTrace.Observability` | OpenTelemetry `Activity` 导出 — 批量 + 实时监听器 |
| `NarrativeTrace.Logging` | `ILogger` 叙事导出 + 关联作用域 |
| `NarrativeTrace.Diagrams` | Mermaid / PlantUML 时序图渲染器 |
| `NarrativeTrace.Clarity` | 命名清晰度分析器、评分器、词典、扫描器 |
| `NarrativeTrace.Testing.Xunit` / `.NUnit` | 在失败时叙述过程的测试夹具 |
| `NarrativeTrace.Cli` | `dotnet-narrativetrace` 全局工具 |
| `NarrativeTrace.MSBuild` | 用于清晰度扫描/门禁的构建目标 |
| `NarrativeTrace.Legacy` | `net48` 兼容层 |

## OpenTelemetry

```csharp
// 批量：把一棵完成的追踪树导出为 Activity Span。
TraceActivityExporter.Export(context.CaptureTrace());

// 实时：把管道的进入/退出事件在发生时转换为 Span。
var listener = new OtelTraceEventListener(new ActivitySource("MyApp"));
bufferedConsumer.Subscribe(listener.OnEvent);
```

## 并发支持

NarrativeTrace 把并发执行 — fork-join(分叉汇合)并行与即发即弃任务 — 当作
追踪树中的一等公民来跟踪。由于 .NET 的上下文通过 `AsyncLocal` 流动(相当于
Java 的 `ThreadLocal`)，每个并行分支都在隔离的子上下文中运行，其追踪随后
带着线程元数据合并回父级。

**Fork-join** — `ForkJoinGroup` 并行运行各分支、汇合它们，并用共享的
`groupId` 把它们的追踪嫁接到父节点之下：

```csharp
var fork = ForkJoinGroup.Create(context);
_ = fork.Fork(iso => NarrativeTraceProxy.Create<IProductCatalogService>(catalog, iso)
    .LookupPrice("book-123"));
_ = fork.Fork(iso => { NarrativeTraceProxy.Create<IInventoryService>(inventory, iso)
    .Reserve("book-123", 2); return true; });
await fork.JoinAsync();
```

**即发即弃** — `FireAndForgetGroup` 会在父级中嫁接一个启动器节点，并通过
`groupId` 关联后台的子追踪；父级不等待即可继续：

```csharp
var group = FireAndForgetGroup.Create(context, "OrderService");
group.Launch(iso => NarrativeTraceProxy.Create<INotificationService>(notifier, iso)
    .NotifyOrderPlaced("cust-1", "ORD-00001"));
```

渲染出的 Markdown 会包含 `⑂ fork [n tasks]` / `⑃ join` 标记、一个
`⤳ fire-and-forget` 启动器、线程名称以及 join 的墙钟时间。完整的实战场景 —
先并行查价/查库存，再发一条异步通知 — 见
[`ConcurrencyScenario`](examples/NarrativeTrace.Examples.ECommerce/ConcurrencyScenario.cs)，
并有特征测试断言共享的 `groupId`、启动器节点，以及 JSON 导出中的并发字段。
若需要在 `await` 之间做环境式传播，`AsyncNarrativeContext.RunAsync` 会把
后续工作挂接到同一条追踪上。

## CLI：`dotnet-narrativetrace`

```bash
dotnet tool install --global NarrativeTrace.Cli

# 为编译好的程序集的命名清晰度评分(仅反射 — 绝不执行它)：
dotnet-narrativetrace clarity-scan --assembly bin/MyApp.dll --output-dir clarity

# 当清晰度低于阈值时让 CI 失败：
dotnet-narrativetrace clarity-check --results clarity/clarity-scan-results.json \
    --min-score 0.7 --max-high-issues 0
```

### MSBuild 集成

引用 `NarrativeTrace.MSBuild`，从构建流程驱动同一道门禁：

```xml
<PropertyGroup>
  <ClarityMinScore>0.7</ClarityMinScore>
  <ClarityMaxHighIssues>0</ClarityMaxHighIssues>
  <ClarityWarnOnly>false</ClarityWarnOnly>
</PropertyGroup>
```

```bash
dotnet build /t:ClarityCheck   # 扫描 + 门禁；通过 stamp 文件实现增量
```

## 配置

每个开关都可以通过 `NARRATIVETRACE_*` 环境变量解析
(`NARRATIVETRACE_LEVEL`、`NARRATIVETRACE_OUTPUT`、`NARRATIVETRACE_FORMAT`
……)。捕获级别由粗到细依次为：`Off`、`Errors`、`Summary`、`Narrative`、
`Detail`。

## 文档

从这里开始：

- [最初 10 分钟](documentation/zh-CN/前10分钟.md) — 一个小小的服务、一个测试，每一步都有真实输出
- [选择集成方式](documentation/zh-CN/选择集成方式.md) — 你需要哪个模块，以决策图的形式呈现
- [故障排查](documentation/zh-CN/故障排查.md) — 症状 → 原因 → 修复，覆盖大家实际会遇到的失败模式
- [应提交的内容](documentation/zh-CN/应提交的内容.md) — 哪些生成的文件是一次性产物，哪些需要纳入评审
- [隐私与脱敏](documentation/zh-CN/隐私与脱敏.md) — 逐行的脱敏契约，对照代码验证过

面向任务的指南位于
[`documentation/guides/zh-CN/`](documentation/guides/zh-CN/)：

- [安装](documentation/guides/zh-CN/安装指南.md) — 包与集成方式
- [配置](documentation/guides/zh-CN/配置指南.md) — 级别、环境变量、DI、MSBuild、脱敏
- [特性](documentation/guides/zh-CN/特性指南.md) — `[Narrated]`、`[OnError]`、`[NotTraced]`、`[Traced]`、`[NarrativeSummary]`
- [依赖注入](documentation/guides/zh-CN/依赖注入指南.md) — MS.DI 容器中的命名空间自动包装
- [ASP.NET Core 集成](documentation/guides/zh-CN/ASP.NET-Core集成指南.md) — 请求中间件、导出器、用户上下文
- [清晰度](documentation/guides/zh-CN/清晰度指南.md) — 评分模型与 CI 门禁
- [MSBuild 与 CLI](documentation/guides/zh-CN/MSBuild与CLI指南.md) — `dotnet-narrativetrace` 子命令与构建时的清晰度门禁

深入了解：

- [功能指南](documentation/feature-guide.md)(仅英文) — 每个功能的规范状态表(免费/Pro/开发中/已规划)，并附有每个已发布行背后的代码
- [安全工具](documentation/security-tooling.md)(仅英文) — 扫描工具清单，以及哪些会作为构建门禁、哪些按计划任务运行
- [安全测试](documentation/security-testing.md)(仅英文) — 在每个 NarrativeTrace 实现之间保持一致的模糊测试/属性测试套件

面向 AI 消费者(仅英文)：[`llms.txt`](documentation/guides/llms.txt) 与
[`llms-full.md`](documentation/guides/llms-full.md)。

## 目标框架

- 现代运行时：`net10.0`
- 兼容性：`netstandard2.0`
- 遗留：`net48`(位于 `NarrativeTrace.Legacy`)

## 演示

观看示例运行的最快方式：一条命令，实时的 `→ ← !!` 叙述按调用深度着色并缩进，
每个场景之后停一下，并附上一段说明该场景的追踪是如何接线的：

```bash
./demo.sh                                 # 交互式选择器
./demo.sh --example ecommerce             # 非交互式；--list 列出全部示例
./demo.sh --example ecommerce --classic   # 同一次运行，以传统的带时间戳日志呈现
./demo.sh --example ecommerce --no-pause  # 一口气播完，不停顿(管道和 CI 得到的就是这个)
```

`./build.sh Demo --example <名称> --no-pause` 与 `./build.sh RunExamples` 从构建
中运行同样的内容；`demo.ps1` 是轻量的 PowerShell 封装。共有四个示例 ——
`ecommerce`、`clarity`、`minecraft` 和 `library`(F#)—— 每个也都可以用
`dotnet run --project examples/<项目>` 单独运行。项目一览以及每个示例教什么，
见 [`examples/自述文件.md`](examples/自述文件.md)。

## 构建与测试

```bash
dotnet build NarrativeTrace.sln
dotnet test NarrativeTrace.sln
```

或者通过 [NUKE](https://nuke.build)(`./build.sh`、`build.ps1`、
`build.cmd`)：

```bash
./build.sh Test        # 运行完整测试套件
./build.sh Verify      # clean + format + analyze + test + coverage + metrics + benchmark
./build.sh Coverage    # Coverlet cobertura 报告
./build.sh Mutation    # Stryker.NET 变异测试
./build.sh Pack        # 生成 NuGet 包
```

### 质量门禁(强制执行)

格式化(`dotnet format`)、静态分析(Roslyn + SonarAnalyzer，含通过 S138
实施的 20 行方法上限，以及完整的 CA5xxx 安全规则类别)、密钥扫描(gitleaks，
预提交钩子扫描暂存差异，外加完整历史记录的全量扫描)、测试(xUnit / NUnit)、
覆盖率(Coverlet)、变异测试(Stryker.NET)与基准回归(BenchmarkDotNet)都会
作为构建门禁。Semgrep 的 OSS C# 规则集与依赖漏洞扫描(OSV-Scanner、
`dotnet list package --vulnerable`)在计划任务/合并请求的 CI 层级运行——参见
[`documentation/security-tooling.md`](documentation/security-tooling.md)。
`tests/BuildScript.Tests` 验证构建自身的行为。CI 在 GitHub Actions 上运行
`./build.sh Verify`(`.github/workflows/ci.yml`)。

## 常见问题

**性能开销有多大？** 见上方的[性能](#性能)一节。

**参数值和返回值是立即序列化(eager)的吗？** 是的 — 值在捕获时按配置的级别
被渲染成字符串。这是有意为之：追踪记录的是调用发生那一刻值*当时*的样子，
而不是之后可能已被修改的视图。参见上方的[隐私与安全](#隐私与安全一屏看懂)
一节，以及完整、经过验证的脱敏契约 —
[隐私与脱敏](documentation/privacy-and-redaction.md)(仅英文)页面。

**追踪会在我的代码里触发副作用吗？** 渲染可能调用一小组有文档记载的成员：
反射内省期间的属性 getter、自定义的 `ToString()`、`[NarrativeSummary]`
成员，以及 `[Narrated]`/`[OnError]` 模板中出现的属性路径。请让它们保持纯净
(getter 无副作用本来就是 .NET Framework 设计准则) — 或者给该成员标注
`[NotTraced]`，这样它的值根本不会被读取。每次调用都是有界的(长度/深度/
元素数上限)、异常隔离的(抛异常的 getter 绝不会让你的业务调用失败)，并且在
调用点立即发生；在 `TracingLevel.Off` 下完全不会发生任何渲染。参见
[特性指南](documentation/guides/zh-CN/特性指南.md)中的纯净性契约。

**它能追踪非接口方法或私有方法吗？** `DispatchProxy` proxy 追踪的是*接口*
调用，因此追踪发生在接口边界上 — 实现内部的私有和 internal 方法调用不会被
单独追踪。这让叙事保持在协作层面(服务对服务)，而这通常正是你想阅读的层面。
若需要更细的粒度，请把行为拆分到接口之后。

**NarrativeTrace 如何与其他包装方法的库(AOP、proxy、契约库)相处？**
它叙述的是业务边界的跨越，而不是实现机制本身 — 生成的装饰器、桥接方法，
或另一个库的 proxy stub 本身永远不会被当作一个独立的追踪帧。当一个契约或
校验库在 `NarrativeTraceProxy` 也在包装的接口上拒绝了某次调用时，这次拒绝
无论哪个 wrapper 先注册，都会作为异常进入追踪 — 唯一取决于注册顺序的是
它附着在哪一帧上(内层还是外层)。一个专门面向不抛异常的违规事件的接入点
已经规划，但尚未构建。目前的
排除手段还很有限：`AddNarrativeTracing` 的 `ExcludeNamespaces` 通过
按点分界的匹配将接口命名空间从 DI 自动包装中排除，而原始 proxy 只会包装
你显式传给 `NarrativeTraceProxy.Create` 的对象。目前还没有针对已知机制
形态(生成的装饰器、编译器生成的类型)的默认排除列表，并且 `DispatchProxy`
一个 proxy 对应一个接口的设计意味着被包装的实例不会暴露它实现的任何
*其他*标记接口 — 这两点仍是路线图上尚未解决的事项。

## 状态

0.1.0 已发布 —— 软件包已上架 NuGet。捕获管道、规范 JSON schema、两层特性模
型、ASP.NET Core 生命周期、DI 接线、OpenTelemetry(批量 + 实时)、清晰度工具链
+ CLI、测试框架集成，以及可运行的示例及其演示启动器均已就位。剩余事项：`net48`
运行时验证。

## 许可

NarrativeTrace 的 API 与输出格式是开放标准(Apache 2.0)。其运行时免费且源码
可得(BSL 1.1，在每个版本发布四年后转为 Apache 2.0)。Pro 为商业授权。

本仓库中的全部内容都属于运行时，采用
[Business Source License 1.1](LICENSE)。你可以将其用于任何生产用途，包括内部
使用，以及用在你向自己客户提供的产品和服务中 —— 唯一的例外是：将 NarrativeTrace
本身，或价值实质上来自于它的产品或服务，作为日志、追踪或代码叙事类产品或服务
提供给第三方。每个版本发布四年后自动转为
Apache 2.0 — 该授权是自动的、按版本生效的，因此你今天采用的版本，其时间表是
可查的。

文档正文采用 [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/)；JSON schema 文件采用 Apache 2.0。

<!-- legal:trademark:begin -->
NarrativeTrace 是 Empower Agile 的商标。本许可证不授予任何商标权利。
<!-- legal:trademark:end -->

### 用大白话说的许可证

本仓库中的一切目前都遵循 Business Source License 1.1；开放的契约层将随 api
拆分到来。

<!-- legal:plain-words:begin -->
**免费运行。** 运行时依据 Business Source License 1.1 源码可得:你可以阅读它、审计它、修改它,
并可免费用于生产环境——包括用在你提供给自己客户的产品与服务中。

**唯一的排除项。** 你不得将 NarrativeTrace 本身——或价值实质上源自它的产品或服务——作为日志、
追踪或代码叙事产品或服务提供给第三方。

**它会在某个日期开放。** 每个发布版本都会在发布四年后转为 Apache 2.0;
确切日期印在该版本的 LICENSE 中。

*这份摘要只是一种礼貌说明,不是许可证本身。LICENSE 文件才是唯一具有约束力的文本;
若二者不一致,以 LICENSE 为准。*
<!-- legal:plain-words:end -->
